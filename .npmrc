; Do not set script-shell here: Linux CI (Netlify) has no powershell.exe.
; If you are on Windows and your project path contains & (e.g. ML&RLLC), run once:
;   npm config set script-shell powershell.exe
; That stores the setting in your user ~/.npmrc only.
